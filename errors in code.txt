виконавши всі тести ми отримали 2 помилки
1) в тесті test_calcucale_avg_valid_student який обчислює середнє значення для учня Soroka I.I. з математики
вирахувавши вручну, ми очікували отримати значення 9.75, а отримали значення 19.5
помилки знаходиться в 19 рядку в функції calcucale_avg

return sum(data_dict[child][subject]) / len(data_dict[child][subject]) * 2

тут середнє значення множиться на 2
щоб виправити цю помилку нам потрібно змінити цей рядок на

return sum(data_dict[child][subject]) / len(data_dict[child][subject])

2) в тесті test_class_by_name_valid який шукає клас для учня і повертає знайдений
ми очікували отримати 8-А , а отримали 8=A
помилка знаходиться в функції class_by_name в 38 рядку

return el[0].replace('-', '=')

тут функція автомитично замінює всі знаки '-' на знаки '='

щоб виправти цю помилку потрібно змінити 38 рядок на

return el[0]
